{
  "name": "pokapal",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "deploy": "source ~/.nvm/nvm.sh && nvm use 22 && npm run build && firebase deploy --only hosting",
    "deploy:hosting": "source ~/.nvm/nvm.sh && nvm use 22 && npm run build && firebase deploy --only hosting",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "firebase": "^12.6.0",
    "vue": "^3.5.24",
    "vue-router": "^4.6.3"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "autoprefixer": "^10.4.22",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.0",
    "vite": "^7.2.2"
  }
}
