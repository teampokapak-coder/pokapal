rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read access for all files
    // Allow authenticated write access for blog images and other uploads
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}

